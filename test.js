let expect = require('chai').expect;
let spliter = require('./server/utils/Helpers.js').splitSubParams;


describe('Spliter', () => {
  it('should exist', () => {
    expect(spliter).to.exist;
  })
})